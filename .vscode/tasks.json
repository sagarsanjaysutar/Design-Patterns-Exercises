{
  "tasks": [
    {
      "type": "cppbuild",
      "label": "Build C++",
      "command": "/usr/bin/g++",
      "args": [
        "-fdiagnostics-color=always",
        "-g",
        "${file}",
        "-o",
        "${workspaceFolder}/Output/${fileBasenameNoExtension}.o"
        // "`pkg-config --cflags --libs opencv`"
      ],
      "options": {
        "cwd": "${fileDirname}"
      },
      "problemMatcher": ["$gcc"],
      "group": "build",
      "detail": "Task generated by Debugger."
    },
    {
      "label": "Run C++",
      "type": "shell",
      "command": "${workspaceFolder}/Output/${fileBasenameNoExtension}.o",
      "options": {
        "cwd": "${fileDirname}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "presentation": {
        "reveal": "always",
        "panel": "new"
      },
      "dependsOn": ["Build C++"],
      "problemMatcher": []
    }
  ],
  "version": "2.0.0"
}
